import type { Metadata } from "next";
import localFont from "next/font/local";
import "./globals.css";

const geistSans = localFont({
  src: "./fonts/GeistVF.woff",
  variable: "--font-geist-sans",
  weight: "100 900",
});
const geistMono = localFont({
  src: "./fonts/GeistMonoVF.woff",
  variable: "--font-geist-mono",
  weight: "100 900",
});

export const metadata: Metadata = {
  title: "Hex Color Visualizer",
  description: "Hex Code Visualizer is a simple, intuitive tool designed to help developers and creatives easily compare and customize colors. Whether you're fine-tuning a UI design or exploring color palettes for your next project, this app provides a straightforward way to visualize, organize, and adjust colors.",
  authors: [{ name: "Sabrina Powell", url: "https://sabrinapowell-projects.com/" }]
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
